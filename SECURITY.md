# 安全与隐私

## 插件发送什么数据

默认只向用户配置的 LLM Endpoint 发送**当前候选页中需要翻译的中文候选短句**。

插件不会主动发送：

- 密码或敏感输入框内容；
- 已上屏的历史文本；
- 周边文本；
- 剪贴板；
- 当前窗口标题或应用信息；
- 文件内容。

密码/敏感输入场景由 Fcitx5 CapabilityFlag 检测并直接跳过。

## API Key

用户配置位于：

```text
~/.config/fcitx5/conf/english-hint.conf
```

开发安装脚本会将该文件权限收紧到 `0600`。请不要把真实 API Key 提交到 Git 仓库、Issue 或日志中。

## 缓存

翻译缓存位于：

```text
~/.cache/fcitx5-english-hint/cache.bin
```

缓存文件权限为 `0600`，内容包括用户输入过并成功翻译的候选短句及对应英文。如果你不希望保留这些内容，可以在退出 Fcitx5 后删除该文件。

## 漏洞报告

如果发现可能导致敏感输入泄露、任意代码执行或越权文件访问的问题，请不要先公开提交利用细节。建议通过 GitHub Security Advisory 私下报告。
